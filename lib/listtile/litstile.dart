import 'package:flutter/material.dart';

class MyLitstile extends StatelessWidget {
  const MyLitstile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue, child: Center(child: Text("Nasir")));
  }
}
